# Pattern 25 example uses Python stdlib only.
# For LangChain LLM cache (InMemoryCache / SQLiteCache), install langchain-core from the repo root.
# For provider prompt caching, use the official Anthropic / OpenAI SDKs per book examples.
